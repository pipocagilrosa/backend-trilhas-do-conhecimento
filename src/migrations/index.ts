import { AddFavoriteToUserCourse1789516800000 } from './1789516800000-AddFavoriteToUserCourse';

/**
 * Registro das migrations, em ordem de execucao.
 *
 * A lista e explicita de proposito: um glob (`migrations/*.{ts,js}`) tambem
 * casaria com os arquivos `.d.ts` gerados em dist/ e quebraria o boot em
 * producao. Ao criar uma migration nova, importe-a e adicione aqui.
 */
export const migrations = [AddFavoriteToUserCourse1789516800000];
